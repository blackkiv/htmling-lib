package com.github.blck.htmling.enums;

public enum HTMLTag {

  /**
   * Specific an anchor (Hyperlink)Use for link in internal/external web documents.
   */
  A,

  /**
   * Describes an abbreviation (acronyms)
   */
  ABBR,

  /**
   * Describes an address information
   */
  ADDRESS,

  /**
   * Defines an area in an image map
   */
  AREA,

  /**
   * Defines an article
   */
  ARTICLE,

  /**
   * Describes contain set(or write) on aside place in page contain
   */
  ASIDE,

  /**
   * Specific audio content
   */
  AUDIO,

  /**
   * Specific text weight bold
   */
  B,

  /**
   * Define a base URL for all the links with in a web page
   */
  BASE,

  /**
   * Specific direction of text display
   */
  BDO,

  /**
   * Specifies a long quotation
   */
  BLOCKQUOTE,

  /**
   * Defines a main section(body) part in HTML document
   */
  BODY,

  /**
   * Specifies a press/push button
   */
  BUTTON,

  /**
   * Specifies the display graphics on HTML web document
   */
  CANVAS,

  /**
   * Define a table caption
   */
  CAPTION,

  /**
   * Specifies a text citation
   */
  CITE,

  /**
   * Specifies computer code text
   */
  CODE,

  /**
   * Specifies a column within a <colgroup> element in table
   */
  COL,

  /**
   * Defines a group of one or more columns inside table
   */
  COLGROUP,

  /**
   * Define a command button, invoke as per user action
   */
  COMMAND,

  /**
   * Define a list of pre-defined options surrounding <input> tag
   */
  DATALIST,

  /**
   * Defines a definition description in a definition list
   */
  DD,

  /**
   * Specific text deleted in web document
   */
  DEL,

  /**
   * Define an additional details hide or show as per user action
   */
  DETAILS,

  /**
   * Define a definition team
   */
  DFN,

  /**
   * Define a division part
   */
  DIV,

  /**
   * Define a definition list
   */
  DL,

  /**
   * Define a definition team
   */
  DT,

  /**
   * Define a text is emphasized format
   */
  EM,

  /**
   * Define an embedding external application using a relative plug-in
   */
  EMBED,

  /**
   * Defines a grouping of related form elements
   */
  FIELDSET,

  /**
   * Represents a caption text corresponding with a figure element
   */
  FIGCAPTION,

  /**
   * Represents self-contained content corresponding with a <figcaption> element
   */
  FIGURE,

  /**
   * Defines a footer section containing details about the author, copyright, contact us, sitemap,
   * or links to related documents.
   */
  FOOTER,

  /**
   * Defines a form section that having interactive input controls to submit form information to a
   * server.
   */
  FORM,

  /**
   * Defines a Headings level from 1 to 6 different sizes.
   */
  H1, H2, H3, H4, H5, H6,

  /**
   * Defines header section of HTML document.
   */
  HEAD,

  /**
   * Defines as a container that hold introductory content or navigation links.
   */
  HEADER,

  /**
   * Define a document is a HTML markup language
   */
  HTML,

  /**
   * Defines an italic format text
   */
  I,

  /**
   * Defines an inline frame that embedded external content into current web document.
   */
  IFRAME,

  /**
   * Used to insert image into a web document.
   */
  IMG,

  /**
   * Define a get information in selected input
   */
  INPUT,

  /**
   * Used to indicate text that is inserted into a page and indicates changes to a document.
   */
  INS,

  /**
   * Used to identify text that are represents keyboard input.
   */
  KBD,

  /**
   * Used to caption a text label with a form <input> element.
   */
  LABEL,

  /**
   * Used to add a caption (title) to a group of related form elements that are grouped together
   * into the <fieldset> tag.
   */
  LEGEND,

  /**
   * Define a list item either ordered list or unordered list.
   */
  LI,

  /**
   * Used to load an external stylesheets into HTML document.
   */
  LINK,

  /**
   * Defines a clickable image map.
   */
  MAP,

  /**
   * Used to highlighted (marked) specific text.
   */
  MARK,

  /**
   * Used to display an unordered list of items/menu of commands.
   */
  MENU,

  /**
   * Used to provide structured metadata about a web page.
   */
  META,

  /**
   * Used to measure data within a given range.
   */
  METER,

  /**
   * Used to define group of navigation links.
   */
  NAV,

  /**
   * Used to provide a fall-back content to the browser that does not support the JavaScript.
   */
  NOSCRIPT,

  /**
   * Used to embedded objects such as images, audio, videos, Java applets, and Flash animations.
   */
  OBJECT,

  /**
   * Defines an ordered list of items.
   */
  OL,

  /**
   * Used to create a grouping of options, the related options are grouped under specific headings.
   */
  OPTGROUP,

  /**
   * Represents option items within a <select>, <optgroup> or <datalist> element.
   */
  OPTION,

  /**
   * Used for representing the result of a calculation.
   */
  OUTPUT,

  /**
   * Used to represent a paragraph text.
   */
  P,

  /**
   * Provides parameters for embedded object element.
   */
  PARAM,

  /**
   * Used to represent preformatted text.
   */
  PRE,

  /**
   * Represents the progress of a task.
   */
  PROGRESS,

  /**
   * Represents the short quotation.
   */
  Q,

  /**
   * Used to provide parentheses around fall-back content to the browser that does not support the
   * ruby annotations.
   */
  RP,

  /**
   * Specifies the ruby text of ruby annotation.
   */
  RT,

  /**
   * Used to represent a ruby annotation.
   */
  RUBY,

  /**
   * Text display in strikethrough style.
   */
  S,

  /**
   * Represents text that should be interpreted as sample output from a computer program.
   */
  SAMP,

  /**
   * Defines client-side JavaScript.
   */
  SCRIPT,

  /**
   * Used to divide a document into number of different generic section.
   */
  SECTION,

  /**
   * Used to create a drop-down list.
   */
  SELECT,

  /**
   * Used to make the text one size smaller.
   */
  SMALL,

  /**
   * Used to specify multiple media resources.
   */
  SOURCE,

  /**
   * Used to grouping and applying styles to inline elements.
   */
  SPAN,

  /**
   * Represents strong emphasis greater important text.
   */
  STRONG,

  /**
   * Used to add CSS styles to an HTML document.
   */
  STYLE,

  /**
   * Represents inline subscript text.
   */
  SUB,

  /**
   * Represents inline superscript text.
   */
  SUP,

  /**
   * Used to define a table in an HTML document.
   */
  TABLE,

  /**
   * Used for grouping table rows.
   */
  TBODY,

  /**
   * Used for creates standard data cell in HTML table.
   */
  TD,

  /**
   * Create multi-line text input.
   */
  TEXTAREA,

  /**
   * Used to adding a footer to a table that containing summary of the table data.
   */
  TFOOT,

  /**
   * Used for creates header of a group of cell in HTML table.
   */
  TH,

  /**
   * Used to adding a header to a table that containing header information of the table.
   */
  THEAD,

  /**
   * Represents the date and/or time in an HTML document.
   */
  TIME,

  /**
   * Represents title to an HTML document.
   */
  TITLE,

  /**
   * Defines a row of cells in a table.
   */
  TR,

  /**
   * Represents text tracks for both the <audio> and <video> tags.
   */
  TRACK,

  /**
   * Represents underlined text.
   */
  U,

  /**
   * Defines an unordered list of items.
   */
  UL,

  /**
   * Represents a variable in a computer program or mathematical equation.
   */
  VAR,

  /**
   * Used to embed video content.
   */
  VIDEO,

  /**
   * Defines a word break opportunity in a long string of text.
   */
  WBR;


  @Override
  public String toString() {
    return this.name().toLowerCase();
  }
}
